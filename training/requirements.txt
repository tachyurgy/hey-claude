# Dependencies for training openWakeWord models (torch + torchaudio are installed
# separately from the CPU index in the workflow). Pinned where it matters:
# openwakeword 0.6.0 is the API train_wakewords.py + train.py target.
openwakeword==0.6.0
piper-tts
torchinfo
torchmetrics
audiomentations
torch-audiomentations
acoustics
pronouncing
mutagen
webrtcvad
datasets
speechbrain
# The training stack predates numpy 2 / scipy 1.15. scipy 1.15 removed
# scipy.special.sph_harm, which the `acoustics` dep imports at module load.
scipy<1.15
numpy<2
soundfile
tqdm
pyyaml
